# Mini-RLHF-PPO Weekly Report

**Generated:** {{ timestamp }}

---

## Configuration Summary

| Parameter | Value |
|-----------|-------|
| Base model | `{{ config.model_name }}` |
| Seed | {{ config.seed }} |
| SFT n_train | {{ config.get('n_train', 'N/A') }} |
| RM pairs | {{ config.get('n_rm_pairs', 'N/A') }} |
| PPO steps | {{ config.get('n_ppo_steps', 'N/A') }} |

---

## Stage 1 — Supervised Fine-Tuning (SFT)

{% if sft_loss is not none %}
- **Final training loss:** {{ "%.4f" | format(sft_loss) }}
{% else %}
- No SFT metrics recorded.
{% endif %}

**What happened:** The base model was fine-tuned on GSM8K using LoRA (r={{ config.get('lora', {}).get('r', 8) }}).
Only the answer tokens contribute to the cross-entropy loss; prompt tokens are masked with `-100`.

---

## Stage 2 — Reward Model (RM)

{% if rm_loss is not none %}
- **Final training loss:** {{ "%.4f" | format(rm_loss) }}
- **Validation accuracy (chosen > rejected):** {{ "%.1f" | format(rm_val_acc * 100) }}%
{% else %}
- No RM metrics recorded.
{% endif %}

**What happened:** A DistilBERT classifier was trained with Bradley-Terry pairwise loss:
`L = -log σ(r_chosen − r_rejected)`.

---

## Stage 3 — PPO with KL Regularization

{% if ppo_mean_reward is not none %}
- **Mean final reward:** {{ "%.4f" | format(ppo_mean_reward) }}
- **Mean KL (last 20 steps):** {{ "%.4f" | format(ppo_mean_kl) }}
{% else %}
- No PPO metrics recorded.
{% endif %}

**What happened:** The SFT policy was updated via PPO. A frozen copy of the SFT model
served as the reference for KL penalty: `r_total = r_RM − β · KL(π || π_ref)`.

---

## Evaluation Results

| Model | Accuracy | Avg. Length |
|-------|----------|-------------|
{% for row in eval_rows %}
| {{ row.model }} | {{ "%.1f" | format(row.accuracy * 100) }}% | {{ "%.1f" | format(row.avg_length) }} |
{% endfor %}

---

## Commentary

{{ commentary }}

---

*Report auto-generated by `src/report/make_report.py`.*
